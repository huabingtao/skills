---
name: danke-strategy-skill
description: 专为《弹壳特攻队》攻略提供一键美化排版与微信公众号发布功能。当用户要求“美化”、“排版”或“发布到微信草稿箱”时自动唤醒，接管从 Markdown 格式化、图床上传到自动生成微信草稿的完整工作流。
---

# 弹壳攻略美化指令集 (Orchestrator)

## 1. 核心定位 (Scope)
其唯一任务是接收一段现有的攻略文本，通过**交互式步骤**引导用户完成从“文本美化”、“配图映射”到“HTML 编译与发布”的完整流程。

## 2. 触发条件 (Triggers)
仅在用户指令中包含以下关键词时激活：
- **美化攻略**、**美化**、**优化**、**优化攻略**、**排版**、**排版攻略**、**整理格式**、**加图**、**微信转换**、**公众号格式**。

## 3. 交互式多阶段工作流 (Interactive Multi-stage Workflow)
为了保证产出质量，本技能包采用分步确认机制。每一阶段处理完毕后，必须展示阶段性成果，并询问用户“继续下一步”或“进行修改”。只有当用户确认后，方可进入下一阶段。

---

### 阶段一：文本整理与数值高亮 (Text Cleanup & Numeric Highlighting)
1. **输入处理**：读取用户提供的原始 Markdown 文本。
2. **规范化加粗**：将文中的装备、技能、宠物、道具等专有名词包裹为 `**【名称】**`（严格按照 `packs/danke/formatting_rules.md` 中的定义）。
3. **数值高亮**：根据 `packs/danke/highlight_rules.json` 正则匹配文中的数字与数值，为其添加对应的 HTML 颜色标签（攻击/伤害标红，生命/防御标蓝，冷却/范围标绿）。
4. **用户反馈**：
   * **展示内容**：处理后的高亮 Markdown 文本。
   * **询问问题**：*“数值高亮和名词强调已完成，您看是否满意？输入【继续】将进入阶段二（智能配图），或者输入您的修改意见。”*

---

### 阶段二：智能配图与样式注入 (Image Mapping & Layout Selection)
1. **行内图标简写（推荐）**：对于需要"加粗文字 + 行内小图标"的技能、道具、宠物名称，优先使用 `{{名称}}` 简写语法。编译器会自动展开为 `**名称**![名称](img://名称){type=icon}`。例如 `{{共鸣伤害}}` → 加粗"共鸣伤害" + 24px图标。
2. **大图与特殊布局配图**：对于大图截图、卡片展示等，使用标准语法 `![图片名](img://图片名){type=样式名}`，根据文章语境添加 `{type=card}`、`{type=banner}`、`{type=grid2}`、`{type=center;w=宽度}` 等排版后缀。
3. **关键词配图补充**：根据 `packs/danke/image_mapping.json` 图片字典，对无法使用简写语法的场景（如星级标记 `5[红星](img://红星){type=icon}`），手动注入图片引用。
4. **用户反馈**：
   * **展示内容**：注入图片标签后的 Markdown 文本。
   * **询问问题**：*“自动配图与布局样式已注入完成，您看是否满意？输入【继续】将进入阶段三（微信 HTML 编译），或者输入您的修改意见。”*

---

### 阶段三：编译微信 HTML 与外链处理 (WeChat HTML Compilation)
1. **HTML 编译**：在后台调用 `scripts/compile.py --pack packs/danke` 编译成带内联 CSS 样式的 HTML。
2. **样式适配**：自动加载 `themes/default.css` 注入到 HTML 的 style 属性，并剔除图片宽高属性防拉伸。
3. **外链与注音**：将非微信的外链转换为脚注，处理 `[文字]{注音}` 为 `<ruby>` 标签。
4. **用户反馈**：
   * **展示内容**：提供编译后的 HTML 预览或代码片段，并指出生成的 HTML 文件路径（如 `test/test_optimization_wechat.html`）。
   * **询问问题**：*“微信 HTML 已成功生成，外链和图片宽高已做安全处理。您看是否满意？输入【继续】将进入阶段四（一键发布至微信草稿箱），或者输入您的修改意见。”*

---

### 阶段四：发布至微信公众号 (WeChat Draft Publishing)
1. **草稿生成**：在后台调用 `scripts/publish.py` 自动上传文章中的本地图片素材，并通过 MD5 缓存去重，最终在公众号后台创建草稿。
2. **用户反馈**：
   * **展示内容**：返回发布的 `MediaID`、生成的文章标题和草稿链接。
   * **询问问题**：*“草稿已成功上传至微信公众号草稿箱！您可以登录微信后台进行终审群发。”*

---

## 4. 核心约束 (Constraints)
- **绝对原文保留**：必须逐字保留用户提供的原始策略事实与核心文字，严禁擅自删减或改写原文。
- **结构原地替换**：严禁改变原文的段落和行内结构。所有美化（加粗、颜色、图片追加）必须在原句位原地进行。
- **严格确认机制**：未经用户明确输入“继续”或同意，严禁跳过当前阶段或直接执行下一阶段任务。
