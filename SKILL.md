---
name: danke-strategy-skill
description: 专为《弹壳特攻队》攻略提供一键美化排版与微信公众号发布功能。当用户要求“美化”、“排版”或“发布到微信草稿箱”时自动唤醒，接管从 Markdown 格式化、图床上传到自动生成微信草稿的完整工作流。
---

# 弹壳攻略美化指令集 (Orchestrator)

## 1. 核心定位 (Scope)
其唯一任务是接收一段现有的攻略文本，通过**全自动步骤**完成从“文本美化”、“配图映射”到“HTML 编译与发布”的完整流程。

## 2. 触发条件 (Triggers)
仅在用户指令中包含以下关键词时激活：
- **美化攻略**、**美化**、**优化**、**优化攻略**、**排版**、**排版攻略**、**整理格式**、**加图**、**微信转换**、**公众号格式**。

## 3. 自动化多阶段工作流 (Automated Multi-stage Workflow)
为了提高效率，本技能包采用全自动执行机制。无需分步确认，直接运行脚本完成从“文本美化”、“配图映射”到“HTML 编译与发布”的完整流程。

---

### 阶段一：文本整理与数值高亮 (Text Cleanup & Numeric Highlighting)
1. **输入处理**：读取用户提供的原始 Markdown 文本。
2. **规范化加粗**：将文中的装备、技能、宠物、道具等专有名词直接加粗为 `**名称**`（去掉方括号，严格按照 `packs/danke/formatting_rules.md` 中的定义）。**注意：无序列表项（以 `-` 开头）内部严禁使用加粗语法，若列表中含有 `**...**` 需自动剥离恢复为纯文本。**
3. **数值高亮**：在本阶段根据 `packs/danke/highlight_rules.json` 正则匹配文中的数字与数值，为其添加对应的 HTML 颜色标签（攻击/伤害标红，生命/防御标蓝，冷却/范围标绿）。后续 HTML 编译阶段默认不重复执行高亮，避免嵌套标记。

---

### 阶段二：智能配图与样式注入 (Image Mapping & Layout Selection)
1. **行内图标简写（推荐）**：对于需要行内小图标的技能、道具、宠物名称，优先使用 `{{名称}}` 简写语法。编译器会自动展开为 `![名称](img://名称){type=icon}`。例如 `共鸣伤害{{共鸣伤害}}` → "共鸣伤害" + 24px图标。
2. **大图与特殊布局配图**：对于大图截图、卡片展示等，使用标准语法 `![图片名](img://图片名){type=样式名}`，根据文章语境添加 `{type=card}`、`{type=banner}`、`{type=grid2}`、`{type=center;w=宽度}` 等排版后缀。如需添加图片下方说明文字，可使用 `caption=说明文字` 参数（例如 `{type=grid2; caption=兑换末世战马碎片}`）。
3. **关键词配图补充**：根据 `packs/danke/image_mapping.json` 图片字典，对无法使用简写语法的场景（如星级标记 `5[红星](img://红星){type=icon}`），手动注入图片引用。

### 阶段三：微信 HTML 编译与外链处理 (WeChat HTML Compilation)
1. **HTML 编译**：在后台调用 `scripts/compile.py --pack packs/danke` 编译成带内联 CSS 样式的 HTML。
2. **样式适配**：自动加载主题 CSS 注入到 HTML 的 style 属性，并剔除图片宽高属性防拉伸；不重复执行 Stage 1 已完成的数值高亮。
3. **外链与注音**：将非微信的外链转换为脚注，处理 `[文字]{注音}` 为 `<ruby>` 标签。

---

### 阶段四：发布至微信公众号 (WeChat Draft Publishing)
1. **草稿生成**：在后台调用 `scripts/publish.py` 自动上传文章中的本地图片素材，并通过 MD5 缓存去重，最终在公众号后台创建草稿。

---

## 4. 核心约束 (Constraints)
- **绝对原文保留**：必须逐字保留用户提供的原始策略事实与核心文字，严禁擅自删减或改写原文。
- **结构原地替换**：严禁改变原文的段落和行内结构。所有美化（加粗、颜色、图片追加）必须在原句位原地进行。
- **严禁无序列表加粗**：无序列表项内部严禁使用加粗语法，若输入文本中无序列表包含加粗，引擎需自动格式化剥离为纯文本，彻底解决微信折行 Bug。
- **固化章节与强调图标规范**：
  * 主标题 H2 Icon：`📋 一、 活动基础信息与任务产出明细` / `📦 二、 开箱节点与道具产出分析` / `💎 三、 进度消耗与钻石补充分析` / `🛒 四、 兑换商店推荐` / `🏆 五、 排行榜奖池与并列提示`
  * 小章节 H3 与强调 Icon：`📊 开箱节点与产出对照` / `🎯 1. 目标: ...` / `💡 说明` / `🚨 周年庆备战提醒`
- **一键全自动执行**：直接调用 `python scripts/interactive_flow.py -y <input_file> --pack packs/danke` 运行全自动流程，无需与用户进行交互确认。

---

## 5. 小红书/抖音图文切图联动 (Xiaohongshu / Douyin Card Slicing)
当用户提出“生成小红书切图”、“公众号转图文”、“小红书卡片生成”、“切图”等诉求时，直接调用 **`article-to-img-skill`** 技能脚本完成原生 HTML 3:4 智能切图：

```bash
python3 /Users/hbt/my-project/skills/article-to-img-skill/scripts/export_cards.py <_wechat.html文件路径>
```
